#include <stdlib.h>
#include <stdio.h>

/**
 * _abs - prints the absluote value of a number
 * @n: number to be compared
 *
 * Return: x the absloute number of n
 */

int _abs(int n)
{
	int x = abs(n);

	return (x);

}
