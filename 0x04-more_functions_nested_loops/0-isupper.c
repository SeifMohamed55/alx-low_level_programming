#include <main.h>
/**
 * _isupper - retuns if the char is upper or not
 * @c: the char
 *
 * Return: true if upper and false otherwise
 */
int _isupper(int c)
{
	if (isupper(c))
		return (true);
	else
		return (false);
}
