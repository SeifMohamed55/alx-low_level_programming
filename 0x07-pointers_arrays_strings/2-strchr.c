#include "main.h"
#include <string.h>
/**
 * _strchr - return a pointer to the first occurence
 * @s: the string
 * @c: the charachter
 *
 * Return: pointer to the first occurence
 */
char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}
