#include "main.h"
#include <string.h>
/**
 * _strcmp - copies a string into other string
 * @src: the src to be copied
 * @dest: the dest str to be copied into
 *
 * Return: char* pointer to dest
 */
int _strcmp(char *dest, char *src)
{
	return (strcmp(dest, src));
}
