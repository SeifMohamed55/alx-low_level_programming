#include "main.h"
#include <string.h>
/**
 * _strcpy - cpy a string into another string
 * @dest: the new copied string
 * @src: the string to be copied
 *
 * Return: pointer to dest
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));

}
